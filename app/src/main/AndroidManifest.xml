<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.green">

    <!-- 连接互联⽹Internet权限 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo"
        android:supportsRtl="true"
        android:theme="@style/NoAppTheme">
        <activity android:name=".ui.activity.MainActivity">

        </activity>
        <activity android:name=".ui.activity.PayModeActivity" />
        <activity android:name=".ui.activity.store.StoreInfoActivity" />
        <activity android:name=".ui.activity.SearchActivity" />
        <activity android:name=".ui.activity.mine.WalletActivity" />
        <activity android:name=".ui.activity.GoodsDetailsActivity" />
        <activity android:name=".ui.activity.homepage.ForgetPasswordActivity" />
        <activity android:name=".ui.activity.homepage.LoginActivity"></activity>
        <activity android:name=".ui.activity.homepage.RegisterActivity" />
        <activity android:name=".ui.activity.SearchListActivity" />
        <activity android:name=".ui.activity.mine.wallet.IntegralActivity" />
        <activity android:name=".ui.activity.mine.wallet.RechargeActivity" />
        <activity android:name=".ui.activity.homepage.AgreementActivity" />
        <activity
            android:name=".ui.activity.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>